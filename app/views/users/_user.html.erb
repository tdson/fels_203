<li>
  <%= link_to user do %>
    <%= avatar_for user, size: 100 %>
  <% end %>
  <div class="info">
    <h4><%= link_to user.name, user %></h4>
    <span>
      <%= t ".join_date" %> <%= user.created_at.strftime Settings.DATE_FORMAT %>
    </span>
  </div>
</li>
